// Generated by CoffeeScript 1.6.2
(function() {
  'use strict';
  var pointsSelectCtrl;

  pointsSelectCtrl = function($scope, pointsService) {
    $scope.selectOptions = {
      query: function(query) {
        return pointsService.query(query.term).$promise.then(function(results) {
          delete results.$promise;
          delete results.$resolved;
          results.forEach(function(result) {
            return result.text = result.fullName;
          });
          console.log(results);
          return query.callback({
            results: results
          });
        });
      }
    };
    $scope.pointsList = pointsService.all();
    return $scope.$watch('pointsSelected', function(point) {
      return console.log(point);
    });
  };
  pointsSelectCtrl.$inject = ['$scope', 'pointsService'];
  return angular.module('flightPlanApp').controller('PointSelectionCtrl', pointsSelectCtrl);
})();
